import React from 'react';
import { ProductList } from '../../../common/ProductList/ProductList';

export const Soap =()=>{
    return(
        <ProductList
            category={"Soap"}
        />
    );
}