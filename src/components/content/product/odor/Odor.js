import React from 'react';
import { ProductList } from '../../../common/ProductList/ProductList';

export const Odor =()=>{
    return(
        <ProductList
            category={"Odor"}
        />
    );
}