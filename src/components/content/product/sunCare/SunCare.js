import React from 'react';
import { ProductList } from '../../../common/ProductList/ProductList';

export const SunCare =()=>{
    return(
        <ProductList
            category={"Sun Care"}
        />
    );
}