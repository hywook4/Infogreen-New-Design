import React from 'react';
import { ProductList } from '../../../common/ProductList/ProductList';

export const BabyWipes =()=>{
    return(
        <ProductList
            category={"Baby Wipes"}
        />
    );
}