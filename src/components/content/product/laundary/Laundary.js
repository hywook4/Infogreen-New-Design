import React from 'react';
import { ProductList } from '../../../common/ProductList/ProductList';

export const Laundary =()=>{
    return(
        <ProductList
            category={"일반 세제"}
        />
    );
}