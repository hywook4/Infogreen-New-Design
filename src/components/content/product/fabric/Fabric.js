import React from 'react';
import { ProductList } from '../../../common/ProductList/ProductList';

export const Fabric =()=>{
    return(
        <ProductList
            category={"Fabric"}
        />
    );
}